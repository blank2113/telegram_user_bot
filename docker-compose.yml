services:
  frontend:
    build: .
    container_name: frontend
    ports:
      - "4000:4000"
    networks:
      - my_project_network
    restart: unless-stopped


networks:
  default:
    external:
      name: my_project_network